"use client";

// Este loader vacío sustituye al skeleton global para que cada sección
// del dashboard gestione su propio estado de carga.
export default function DashboardLoading() {
  return null; // Sin loader global
} 